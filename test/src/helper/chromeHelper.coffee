window.chrome =
  storage:
    local:
      get: ->
      set: ->

  tabs:
    query: ->
    update: ->

  browserAction:
    setBadgeText: ->
    setBadgeBackgroundColor: ->
    setIcon: ->

  contextMenus:
    create: ->

  commands:
    onCommand:
      addListener: ->